Учебные задания по динамическому выделению памяти

Этот раздел содержит набор учебных задач, направленных на освоение механизмов динамического выделения памяти в языке C. Упражнения охватывают работу с одномерными и двумерными массивами, указателями, функциями выделения памяти и операциями перераспределения (realloc).

📌 Перечень заданий

Слияние двух отсортированных массивов
За один проход объединить два отсортированных массива в третий упорядоченный. Возвратить массив в main и вывести на экран.

Поиск подмассива с максимальным значением функции
Найти подмассив, для которого сумма элементов максимальна. Добавить найденный подмассив в конец исходного, расширив массив через realloc.

Работа с указателем на двумерный статический массив
Описать массив arr[2][3], создать указатель, проверить размер единицы хранения, ввести и вывести элементы.

Создание динамических двумерных массивов двумя способами
Ввести данные и вывести их.

Функция, создающая двумерный массив
Выделить память под n × m элементов, вывести адреса и значения элементов, затем освободить память в main.

Копирование строк без отрицательных элементов
Из динамической матрицы сформировать новую матрицу, содержащую только строки без отрицательных элементов (с перераспределением памяти через realloc).

Формирование бинарной матрицы условий
Для каждой строки проверить условия:

все элементы равны нулю

все элементы одинаковые

все элементы чётные

последовательность монотонна

строка — палиндром
Минимальный путь между тремя площадями в графе
По известным расстояниям найти минимальный путь между тремя вершинами и вывести результат.
